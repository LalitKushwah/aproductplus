package com.hotwaxsystems.productplus.network;

/**
 * Created by Tanmay on 29/12/16.
 */

public interface CustomerCalls {
/*

    @POST("api/control/createPosCustomer;jsessionid={sessionId}")
    Call<CustomerDetails> createCustomer (@Header("authenticationToken")String token, @Body CustomerTab.CustomerDetailsToPost data, @Path("sessionId")String sessionId);

    @FormUrlEncoded
    @POST("api/control/customerSearchResults;jsessionid={sessionId}")
    Call<SuggestedCustomerDeatilsList> searchCustomerDetails(@Header("authenticationToken")String token, @Field("keyword") String keyword, @Path("sessionId")String sessionId);

    @POST("api/control/getCustomerDetail;jsessionid={sessionId}")
    Call<CustomerDetailsResponse> getCustomerDetails (@Header("authenticationToken")String token, @Body CustomerTab.PostAddCustomer data, @Path("sessionId")String sessionId);
*/
}
